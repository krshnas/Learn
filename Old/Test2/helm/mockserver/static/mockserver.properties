# Specifies the path to the initialization JSON file
mockserver.initializationJsonPath=/config/initializerJson.json

# Additional configurations (optional, modify based on needs)
mockserver.logLevel=INFO
mockserver.enableCORS=true
